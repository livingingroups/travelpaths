% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augment.R
\name{segment_meta}
\alias{segment_meta}
\alias{group_meta}
\title{Metadata from a segmentation / grouping fit}
\usage{
segment_meta(x, ...)

group_meta(x, ...)
}
\arguments{
\item{x}{a \code{travelpath_fit} object.}

\item{...}{additional arguments currently unused.}
}
\value{
A `data.frame` with `n_groups(x)` rows, or `NULL` when
  the engine provides no per-label metadata.
}
\description{
`segment_meta()` and `group_meta()` return the `meta` slot
(a \code{data.frame} with one row per segment / group, or `NULL`)
with the mode-specific dispatch used by the rest of the package.
}
