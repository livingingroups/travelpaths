% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/w_plot.R
\name{w_plot_phase_parameter}
\alias{w_plot_phase_parameter}
\title{Plot Phase Parameters}
\usage{
w_plot_phase_parameter(
  variable,
  phaselist,
  cols = seq_along(phaselist),
  label = TRUE,
  ...
)
}
\arguments{
\item{variable}{A character string specifying the name of the parameter to plot
(e.g., `"tau"`, `"eta"`).}

\item{phaselist}{A list of phase data containing attributes required for plotting
(e.g., `start`, `end`, `low`, `high`, `hat`).}

\item{cols}{A vector of colors used for shading and lines, corresponding to the phases.
Defaults to `1:length(phaselist)`.}

\item{label}{Logical. If `TRUE`, adds the parameter name as a label at the top of the plot.
Defaults to `TRUE`.}

\item{...}{Additional graphical parameters to pass to the `plot` function.}
}
\value{
Produces a plot of the specified phase parameter, including shaded areas for ranges and
lines for parameter values.
}
\description{
Visualizes a specified phase parameter within a phase list, including parameter ranges and
values. Supports color-coded shading and labeling.
}
\examples{
\dontrun{
# Example usage:
w_plot_phase_parameter("tau", your_phase_list, label = TRUE)
}
}
