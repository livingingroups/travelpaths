% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/w_plot.R
\name{w_plotPhaseList}
\alias{w_plotPhaseList}
\title{Plot Phases and Associated Parameters}
\usage{
w_plotPhaseList(
  phaselist,
  cols = gplots::rich.colors(length(phaselist)),
  plot.parameters = TRUE,
  parameters = NULL,
  plot.legend = TRUE,
  legend.where = "bottomright",
  layout = c("horizontal", "vertical")
)
}
\arguments{
\item{phaselist}{A list containing phase data with attributes `Z` (complex numbers for positions), `time` (timestamps), and phase information.}

\item{cols}{A vector of colors for each phase. Defaults to `gplots::rich.colors(length(phaselist))`.}

\item{plot.parameters}{Logical. If `TRUE`, plots associated phase parameters. Defaults to `TRUE`.}

\item{parameters}{A character vector specifying which parameters to plot (e.g., `c("eta", "tau")`). If `NULL`, defaults to commonly used parameters found in the phase table.}

\item{plot.legend}{Logical. If `TRUE`, displays a legend for phase names and models. Defaults to `TRUE`.}

\item{legend.where}{Position of the legend. Options include `"bottomright"`, `"topleft"`, etc. Defaults to `"bottomright"`.}

\item{layout}{Layout of parameter plots. Either `"horizontal"` or `"vertical"`. Defaults to `"horizontal"`.}
}
\value{
Generates visual plots of phases and associated parameters.
}
\description{
This function visualizes phase data contained within a list, along with optional parameter plots, color-coded by phase. It supports customizable legends and layouts.
}
\examples{
\dontrun{
# Example usage:
phaselist <- your_phase_data # Replace with actual phase data
w_plotPhaseList(phaselist)
}

}
