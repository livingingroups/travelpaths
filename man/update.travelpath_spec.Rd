% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/specification.R
\name{update.travelpath_spec}
\alias{update.travelpath_spec}
\title{Update a travelpath specification object}
\usage{
\method{update}{travelpath_spec}(object, params = list(), fresh = FALSE, ...)
}
\arguments{
\item{object}{A travelpath specification object to be updated.}

\item{params}{A named list of parameters to update. The function will
automatically determine which parameters are main arguments vs engine-specific
arguments based on the specification's formal arguments.}

\item{fresh}{A logical value indicating whether to start with fresh default
values (`TRUE`) or preserve existing parameter values (`FALSE`, default).
When `TRUE`, only the parameters specified in `params` will be set, all others
will revert to defaults.}

\item{...}{Additional named parameters to update. These will be combined with
the `params` list.}
}
\value{
An updated travelpath specification object with the same class as the
  input object.
}
\description{
Update the parameters of a travelpath specification object with new values.
This function allows you to modify the arguments and engine-specific arguments
of an existing travelpath specification.
}
\examples{
spec <- threshold_stops(min_staying_time = 5)
updated_spec <- update(spec, params = list(min_staying_time = 10))
fresh_spec <- update(spec, fresh = TRUE, min_staying_time = 15)
}
