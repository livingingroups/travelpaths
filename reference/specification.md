# Specification Accessor Function

The specification of a given travelpath fit (`"travelpath_fit"`) can be
accessed via the method `'specification'`.

## Usage

``` r
specification(x, ...)
```

## Arguments

- x:

  object from which the specification is extracted.

- ...:

  further arguments currently ignored.

## Value

an object inheriting from `"travelpath_spec"`
